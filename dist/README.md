Put the public read-only lore data for the GitHub Pages viewer here.

From the app, use Settings > Download Website Data, then save or upload that file as:

```text
public/lore-data.json
```

The hosted viewer build loads this JSON file. If it is missing, the viewer falls back to starter data.
